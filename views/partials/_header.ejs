<header>
  <h1>TinyApp</h1>
    <% if (!userInfo) { %>
      <a href='/login'>Login</a> | <a href='/register'>Register</a>
    <% } else { %>
      <p>Logged in as: <%= userInfo %>    <form method="POST" action="/logout"><button type="submit" class="btn btn-info">Logout</button></form></p>
      <a href='/urls'>URLs</a> | <a href='/urls/new'>Create New TinyURL</a>
    <% }; %>
</header>